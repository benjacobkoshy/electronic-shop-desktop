<UserControl x:Class="ElectronicShop.App.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ElectronicShop.App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Customers" Style="{StaticResource PageTitleStyle}" VerticalAlignment="Center"/>
            <Button Content="+ Add Customer" Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding AddCustomerCommand}" HorizontalAlignment="Right"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Customer list -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="16">
                <DockPanel>
                    <TextBox DockPanel.Dock="Top" Style="{StaticResource SearchBoxStyle}"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Search by name or phone..." Margin="0,0,0,12"/>

                    <ListBox ItemsSource="{Binding CustomersView}" SelectedItem="{Binding SelectedCustomer}"
                             BorderThickness="0" Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="8">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Phone}" Style="{StaticResource CaptionStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Customer detail + tickets -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Visibility="{Binding SelectedCustomer, Converter={StaticResource NullToVisibilityConverter}}">
                        <TextBlock Text="{Binding SelectedCustomer.Name}" Style="{StaticResource SectionHeaderStyle}"/>
                        <TextBlock Style="{StaticResource CaptionStyle}">
                            <Run Text="{Binding SelectedCustomer.Phone, Mode=OneWay}"/>
                            <Run Text=" • "/>
                            <Run Text="{Binding SelectedCustomer.Email, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,16,0,12">
                        <TextBlock Text="Service Tickets" Style="{StaticResource CaptionStyle}" VerticalAlignment="Center"/>
                        <Button Content="+ New Ticket" Style="{StaticResource SecondaryButtonStyle}"
                                Padding="12,6" FontSize="12"
                                Command="{Binding AddTicketCommand}" HorizontalAlignment="Right"/>
                    </Grid>

                    <ItemsControl Grid.Row="2" ItemsSource="{Binding SelectedCustomerTickets}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding DeviceDescription}" Style="{StaticResource BodyTextStyle}" FontWeight="SemiBold"/>
                                            <TextBlock Style="{StaticResource CaptionStyle}" Margin="0,4,0,0">
                                                <Run Text="Received: "/>
                                                <Run Text="{Binding ReceivedDate, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <ComboBox Grid.Column="1" Width="150" Padding="8"
                                              ItemsSource="{x:Static vm:CustomersViewModel.StatusOptions}"
                                              SelectedItem="{Binding Status}"
                                              SelectionChanged="StatusComboBox_SelectionChanged"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>