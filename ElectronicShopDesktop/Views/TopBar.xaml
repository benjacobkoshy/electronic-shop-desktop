<UserControl x:Class="ElectronicShop.App.Views.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ElectronicShop.App.Views"
             mc:Ignorable="d" >
    <Border Background="{DynamicResource TopBarBrush}"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
        <Grid Margin="24,0">
            <TextBlock Text="{Binding CurrentPageTitle}"
                       Style="{StaticResource PageTitleStyle}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <ToggleButton Style="{StaticResource ThemeToggleStyle}"
                              Command="{Binding ToggleThemeCommand}"
                              Content="🌓" Margin="0,0,16,0"/>
                <TextBlock Text="Admin" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
