<Window x:Class="ElectronicShop.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectronicShop.App"
        xmlns:views="clr-namespace:ElectronicShop.App.Views"
        mc:Ignorable="d"
        Title="Electronic Shop Manager"
        Height="720" Width="1280"
        MinHeight="600" MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <views:Sidebar Grid.Column="0" Grid.Row="0" Grid.RowSpan="2"/>

        <views:TopBar Grid.Column="1" Grid.Row="0"/>

        <ContentControl Grid.Column="1" Grid.Row="1"
                         Content="{Binding CurrentViewModel}"
                         Margin="16"/>
    </Grid>
</Window>